# Additional dependencies for the Windows GUI and optional vocal separation.
# PyTorch is installed by install_windows.bat before the editable stable-ts
# install so the selected CUDA/CPU build is not replaced later.
gradio>=4.44,<6
demucs>=4.0.1
